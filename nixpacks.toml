[phases.setup]
nixPkgs = ["nodejs_22", "chromium", "nss", "atk", "cups", "gtk3", "alsa-lib", "unzip", "gnutar"]

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
PUPPETEER_EXECUTABLE_PATH = "/run/current-system/sw/bin/chromium"
